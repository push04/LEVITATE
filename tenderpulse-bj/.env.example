SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
GROQ_API_KEY=
WHATSAPP_SESSION_PATH=
NETLIFY_SITE_URL=
CRAWL_CONCURRENCY=5
CRAWL_INTERVAL_MINUTES=30
GROQ_DAILY_QUOTA=14000

# Admin server (server/index.ts)
ADMIN_PORT=4000
# Points at the deployed Netlify email function, e.g.
# https://yoursite.netlify.app/.netlify/functions/send-tender-email
EMAIL_FUNCTION_URL=
EMAIL_FUNCTION_SECRET=

# Netlify function only (netlify/functions/send-tender-email.ts) — set these
# in the Netlify site's own environment variables, not here.
# BREVO_API_KEY=
# EMAIL_FROM=alerts@levitatelabs.online
# EMAIL_FROM_NAME=TenderPulse BJ
